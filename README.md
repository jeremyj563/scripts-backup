```
~$ mkdir backup && cd backup
~/backup$ git clone https://github.com/jeremyj563/scripts-backup.git .

# locale-gen

# apt install -y python-pip
# apt install -y pip
# pip install flask-restful

# iptables -I INPUT -p tcp -m tcp --dport 5000 -j ACCEPT
# service iptables save
# netstat -tulpn

# setcap CAP_NET_BIND_SERVICE=+eip /usr/bin/python2.7

# nano /lib/systemd/system/approval-api.service
# systemctl daemon-reload
# systemctl enable approval-api.service
# systemctl start approval-api.service
# systemctl status approval-api.service
```
